[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  PORT = "3000"
  BASE_PATH = "/"
  NODE_ENV = "production"
